{
  "name": "coap-test-client",
  "private": true,
  "version": "0.0.0",
  "description": "Simple load testing tool for CoAP server",
  "main": "index.js",
  "scripts": {
    "launch-get": "node index.js --type get",    
    "launch-observe": "node index.js --type observe"
  },
  "author": "SemIoT project",
  "license": "ISC",
  "dependencies": {
    "coap": "^0.9.1"
  },
  "devDependencies": {
    "yargs": "^3.12.0",
    "tracer": "^0.7.4"
  }
}
